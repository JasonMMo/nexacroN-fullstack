<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexacro.fullstack.business.domain.file.FileDao">

    <insert id="insert" parameterType="map">
        INSERT INTO FILE_META (FILE_ID, ORIGINAL_NAME, STORED_PATH, CONTENT_TYPE, SIZE_BYTES, UPLOADED_BY)
        VALUES (#{FILE_ID}, #{ORIGINAL_NAME}, #{STORED_PATH}, #{CONTENT_TYPE}, #{SIZE_BYTES}, #{UPLOADED_BY})
    </insert>

    <select id="findById" parameterType="string" resultType="map">
        SELECT FILE_ID, ORIGINAL_NAME, STORED_PATH, CONTENT_TYPE, SIZE_BYTES, UPLOADED_BY, UPLOADED_AT
          FROM FILE_META
         WHERE FILE_ID = #{fileId} AND DELETED = FALSE
    </select>

    <select id="listAll" resultType="map">
        SELECT FILE_ID, ORIGINAL_NAME, STORED_PATH, CONTENT_TYPE, SIZE_BYTES, UPLOADED_BY, UPLOADED_AT
          FROM FILE_META
         WHERE DELETED = FALSE
         ORDER BY UPLOADED_AT DESC
    </select>

</mapper>

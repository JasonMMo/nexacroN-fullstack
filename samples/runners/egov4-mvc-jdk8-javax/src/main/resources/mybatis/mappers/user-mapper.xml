<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nexacro.uiadapter.mapper.UserMapper">

    <select id="selectById" parameterType="string"
            resultType="com.nexacro.uiadapter.domain.User">
        SELECT USER_ID, USER_NAME, EN_NAME, COMP_PHONE, PHONE, CELL_PHONE, COMPANY,
               JOB_POSITION, ASSIGNMENT, OFFICER_YN, FAX, ZIP_CODE, ADDRESS,
               COMP_ZIP_CODE, COMP_ADDRESS, EMAIL, DEPT_ID, PASSWORD
        FROM TB_USER
        WHERE USER_ID = #{userId}
    </select>

    <update id="updateLastLogin" parameterType="string">
        UPDATE TB_USER SET USER_NAME = USER_NAME WHERE USER_ID = #{userId}
    </update>

</mapper>

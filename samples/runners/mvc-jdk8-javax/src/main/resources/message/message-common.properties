# Default (locale-independent) message bundle.
# Resolved by ReloadableResourceBundleMessageSource (basename: classpath:/message/message-common).
# Referenced by NexacroMappingExceptionResolver.defaultErrorMsg (dispatcher-servlet.xml).

# Generic server-side failure message returned in Nexacro envelope when an
# unhandled exception bubbles up through the controller layer.
fail.common.msg=A server-side error occurred. Please retry, or contact the administrator if the problem persists.
